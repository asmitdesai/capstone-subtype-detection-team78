# Handover — 2026-07-28

## 1. Done today

- **4 label disagreements resolved** (methylation cvPTC/fvPTC vs TCGA clinical ICD-O).
  Relabeled BJ-A45K, J8-A3O0, FY-A4B0 → fvPTC (TCGA central pathology re-review
  confirmed them). Excluded FY-A3R9 (review flagged as inconsistent — genuinely
  unresolved). Full reasoning: [`label_corrections_log.txt`](label_corrections_log.txt).
  **Pending:** FY-A4B0's relabel is only logged, not yet applied — it's
  `meth_split=='train'` so it wasn't touched by today's demo-set extraction.
  Apply it whenever the training set gets assembled.

- **PairedDemoSet extracted** → `PairedDemoSet/`: 92 methylation patients
  (69 cvPTC / 23 fvPTC), combined beta-values matrix + `labels.csv` +
  `image_case_ids.csv`. Raw source files untouched (read-only extraction).

- **Resume-safety confirmed.** Default `cp` mode just blindly re-copies
  everything (safe but wasteful). Switched both download scripts to
  `use_s5cmd_sync=True` — verified via truncation test that it detects and
  re-fetches only the corrupted file, leaves the rest of the series alone.

- **7 download batches split** and saved to `download_batches/batch_01.csv`
  … `batch_07.csv`, demo-set cases front-loaded into the earliest batches,
  cv/fv ratio kept roughly consistent per batch.

## 2. Known gap (not blocking)

Only **27 of the 92** demo-set patients have a row in the current 134-case
image subset (`tcga_thca_download_subset.csv`) — the other 65 have no slide
selected yet. Proceeding as-is; tracked as an open item in
[`follow_up_items.txt`](follow_up_items.txt) for after the pipeline works
end-to-end.

## 3. Immediate next step

Run batch_01 (all 20 cases are demo-set):

```
python code/scripts/download_tcga_thca_slides.py Dataset/Datasets/download_batches/batch_01.csv
```

## 4. Then

Run batch_02 through batch_07 the same way, one at a time:

```
python code/scripts/download_tcga_thca_slides.py Dataset/Datasets/download_batches/batch_0N.csv
```

## 5. After all batches are downloaded

Build the DICOM tiling pipeline:
- Use **wsidicom** (not OpenSlide) to read the downloaded DICOM WSIs
- Tissue / quality / ink filtering on tiles
- Macenko stain normalization
